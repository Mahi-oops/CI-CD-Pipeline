name: Testing CI/CD pipeline
on:
 push:
  branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Print a Message
      run: echo "Hello, this is a test for CI/CD pipeline"